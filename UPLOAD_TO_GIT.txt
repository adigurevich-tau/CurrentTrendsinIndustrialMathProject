# Files to upload to GitHub (override current repo)
# Copy these from: /Users/adigur/Cursor/CurrentTrendsinIndustrialMathProject/
#
# Upload each file to the REPO ROOT (same level as README.md).
# Keep bayesian_config.json from the repo (do not delete it).

main.py
README.md
requirements.txt
compare_pruning_methods.py
bayesian_evaluation.py
bayesian_wavelet_pruning_fixed.py
bayesian_score_pruning.py
bayesian_csi_pruning.py

# Optional: replace README on GitHub with the one above.
# Do NOT remove bayesian_config.json (it's already in the repo).
